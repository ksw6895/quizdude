# 공통 환경 변수
# GEMINI 관련 키/엔드포인트
GEMINI_API_KEY=your-gemini-api-key
# 옵션: 기본 모델 재정의 (예: gemini-1.5-pro-latest)
# GEMINI_MODEL=
# 옵션: Gemini API 베이스 URL 재정의
# GEMINI_API_BASE_URL=

# Prisma가 연결할 데이터베이스 URL (Postgres)
DATABASE_URL=postgresql://username:password@localhost:5432/quizdude

# Blob 스토리지 토큰/엔드포인트 (필요 시)
# BLOB_READ_WRITE_TOKEN=
# BLOB_ACCOUNT_ID=
# BLOB_PUBLIC_BASE_URL=

# Render/워커 운영 파라미터
JOB_POLL_INTERVAL_MS=5000
JOB_MAX_ATTEMPTS=3
WORKER_CONCURRENCY=1
# Render 인스턴스 ID가 있으면 주입 (로컬은 자유롭게)
RENDER_INSTANCE_ID=local-dev

# Strict JSON schema 강제 여부 (기본 true)
# relaxed 모드를 쓰려면 true로 설정
# GEMINI_ALLOW_RELAXED_SCHEMA=true

# 기타 서비스 전용 변수 (필요 시 채워넣기)
# GEMINI_API_KEY, DATABASE_URL 외에
# apps/orchestrator, apps/web 등에서 사용하는 플래그가 있다면
# 이 파일 하단에 추가하세요.
